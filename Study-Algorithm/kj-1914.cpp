#include <iostream>
#include <cstring> 
#include <algorithm>
#include <vector>
using namespace std;

/*
	하노이 탑
	장대가 3개
	1번 장대에 n개의 원판이 있다.
	반경이 큰 순서대로 쌓여있다.
	1->3으로 옮긴다.

	1. 한 번에 한 개의 원판만 옮길 수 있다.
	2. 항상 위 원판이 아래 원판보다 작아야한다.

	필요한 이동순서를 출력
	이동횟수는 최소.
	N (1 ≤ N ≤ 100)
*/

int n;

int main()
{
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);

	cin >> n;


	return 0;
}